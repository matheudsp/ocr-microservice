# ==========================================
#  OCR MICROSERVICE - ENVIRONMENT TEMPLATE
# ==========================================
# Copie este arquivo para .env.development (local) ou .env.production (docker)
# e ajuste os valores conforme necessário.

# --- Server Config ---
PORT=3000
# Opções: development | production | test
NODE_ENV=development

# --- Application Settings ---
OCR_THRESHOLD=0.75
QUEUE_CONCURRENCY=1
# Gere uma chave segura ou use uma fixa para dev
# Ex: node -e 'console.log("sk_admin_" + require("crypto").randomBytes(16).toString("hex"))'
API_KEY_ADMIN=sk_admin_changeme

# --- Database (Postgres) ---
POSTGRES_USER=admin
# Ex: node -e 'console.log(require("crypto").randomBytes(16).toString("hex"))'
POSTGRES_PASSWORD=changeme
POSTGRES_DB=ocr_db

# URL de Conexão para o Drizzle ORM
# - Docker: postgresql://admin:password123@postgres:5432/ocr_db?schema=public
# - Local:  postgresql://admin:password123@localhost:5432/ocr_db?schema=public
DATABASE_URL="postgresql://admin:password123@localhost:5432/ocr_db?schema=public"

# --- Redis (BullMQ) ---
# - Docker Compose: use 'redis'
# - Local: use 'localhost'
REDIS_HOST=localhost
REDIS_PORT=6379
# Ex: node -e 'console.log(require("crypto").randomBytes(16).toString("hex"))'
REDIS_PASSWORD=changeme

# --- MinIO (S3 Compatible Storage) ---
# - Docker Compose: use 'minio'
# - Local: use 'localhost'
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_ACCESS_KEY=admin
# Ex: node -e 'console.log(require("crypto").randomBytes(16).toString("hex"))'
MINIO_SECRET_KEY=changeme
MINIO_BUCKET=docs
MINIO_USE_SSL=false

# --- Google Cloud Vision ---
# Caminho para o arquivo JSON de credenciais da conta de serviço
# Garanta que este arquivo exista na pasta secrets/
GOOGLE_APPLICATION_CREDENTIALS="./secrets/gcp-keys.json"